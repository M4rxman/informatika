program 6.40gg ;
var i:real , k, n, a, :integer;
 begin
  WRITELN('k=');
  READLN(k);
  WRITELN('a=');
  READLN(a);
  k := 2*k-1
  a :2*a+1
  while k=<21 DO
   begin
   n:= k * a
   end;
  write(n);
  end.